# 第六题有些难度,在想了
>预计会在9号晚更迭算法
# 1
```c
#include "stdio.h"
int main()
{
    int x=0;
    for (;;x++)
        if(x%11==10)
            if(x%6==5)
                if(x%7==4)   
                    if(x%5==1)
                    {
                        printf("%d",x);
                        goto end;
                    }
    end:return 0;
}
```
# 2 
```c
#include "stdio.h"
int main()
{
    int N,P,i;
    scanf("%d",&P); 
    for(i = 0;i<P;i++)
    {
        scanf("%d",&N);
        if((N/100000%10+N/10000%10+N/1000%10)==(N/100%10+N/10%10+N%10))
            printf("You are lucky!\n");
        else
            printf("Wish you good luck.\n");
    }
}
```

# 3
```c
#include "stdio.h"
int main()
{
    int i,N,T,sum,S=0;
    scanf("%d",&N);
    for(i=1;i<=N;i++)
    {
        sum = 1;
        for(T=1;T<=i;T++)
        {
            sum*=T;
        }
        S+=sum;
    }
    printf("%d",S);
}
```
# 4

```c

#include "stdio.h"
int main()
{
    int NUM,N,i,t,D[10],T[10];
    scanf("%d",&N); 
    for(t = 0;t<N;t++)
    {
        scanf("%d",&NUM);
        for(i=2;i<=9;i++)
        {
            D[i]=NUM*i;
            T[i]=D[i]/100000%10+D[i]/10000%10+D[i]/1000%10+D[i]/100%10+D[i]/10%10+D[i]%10;
        }
        if(T[2]==T[3]&&T[3]==T[4]&&T[4]==T[5]&&T[5]==T[6]&&T[6]==T[7]&&T[7]==T[8]&&T[8]==T[9])
            printf("%d\n",T[2]);
        else
            printf("NO\n");
    }
}
```

# 5
```c
#include "stdio.h"
int main()
{
    int NUM=1,N,i,i2,sp=-1,t=0;
    char S;
    scanf("%d %c",&N,&S);  
    while ((N-t)>0)
    {
        
        NUM+=2;
        t+=NUM*2;
    }
    t=t-NUM*2;
    NUM=NUM-2;
    for(;NUM>1;NUM=NUM-2)
        {
            sp+=1;
            for(i2=0;i2<sp;i2++)
                printf(" ");
            for(i=1;i<=NUM;i++)
                printf("%c",S);
            printf("\n");
        }
        sp+=1;
    for(;sp>=0;sp=sp-1)
    {
        for(i2=0;i2<sp;i2++)
            printf(" ");
        for(i=1;i<=NUM;i++)
            printf("%c",S);
        printf("\n");
        NUM+=2;
    }
    printf("%d\n",N-t-1);
}
```